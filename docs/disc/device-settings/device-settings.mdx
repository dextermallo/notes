---
sidebar_position: 4
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Device Settings

Sync between personal devices

key items:
- use container
- use same package manager

## 1. Setting up a macOS / Linux Device

### 1.1. Application

1. General
    - [Microsoft Edge](https://www.microsoft.com/en-us/edge)
    - [Visual Studio Code](https://code.visualstudio.com/)
        - install VSCode command `code` via searching in VSCode with shift + cmd + P
    - [Raycast (macOS)](https://www.raycast.com/)
    - [Notion (macOS)](https://www.notion.so/)
2. Optional
    - [AppCleaner](https://freemacsoft.net/appcleaner/)
    - [Horo](https://apps.apple.com/us/app/horo-timer-for-menu-bar/id1437226581?mt=12)
    - [Sublime Text](https://www.sublimetext.com/)
    - [IINA](https://iina.io/)
    - [Postman](https://www.postman.com/)
    - [VirtualBox](https://www.virtualbox.org/)
    - [CheatSheet](https://www.mediaatelier.com/CheatSheet/)
    - [Fig](https://fig.io/)
    - [Turbo Boost Switcher](http://tbswitcher.rugarciap.com/)
    - [DBeaver](https://dbeaver.io/)
    - [Docker](https://www.docker.com/)

## 1.2. Package Manager

<Tabs
values={[
	{label: 'macOS', value: 'macos'},
	{label: 'Linux', value: 'linux'}
]}>
<TabItem value="macos">
```sh
# REF: https://brew.sh/
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```
</TabItem>

<TabItem value="linux">
```md
@TODO: WIP
```
</TabItem>
</Tabs>

1. install brew
    ```sh
    # https://docs.brew.sh
    xcode-select --install

    mkdir homebrew && curl -L https://github.com/Homebrew/brew/tarball/master | tar xz --strip 1 -C homebrew

    eval "$(homebrew/bin/brew shellenv)"
    brew update --force --quiet
    chmod -R go-w "$(brew --prefix)/share/zsh"
    ```
2. install via Brewfile
    ```sh
    brew bundle
    ```
3. install oh my zsh & powerlevel10k
    ```sh
    # install oh my zsh
    # https://ohmyz.sh/#install
    sh -c "$(curl -fsSL https://raw.github.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

    # install powerlevel10k
    # https://github.com/romkatv/powerlevel10k#meslo-nerd-font-patched-for-powerlevel10k
    git clone --depth=1 https://github.com/romkatv/powerlevel10k.git ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k
    # install font
    https://github.com/romkatv/powerlevel10k#manual-font-installation
    
    # plugins
    # https://github.com/unixorn/awesome-zsh-plugins#plugins
    # https://github.com/zsh-users/zsh-syntax-highlighting/blob/master/INSTALL.md
    # https://github.com/marlonrichert/zsh-autocomplete
    ```
4. install nvm
    ```sh
    # https://github.com/nvm-sh/nvm#installing-and-updating
    curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.1/install.sh | bash

    # update ~/.zshrc
    export NVM_DIR="$([ -z "${XDG_CONFIG_HOME-}" ] && printf %s "${HOME}/.nvm" || printf %s "${XDG_CONFIG_HOME}/nvm")"
    [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh" # This loads nvm
    ```

### 1.2. Terminals

### 1.3. Dev Utils

### 1.4. Fine-tuning

<Tabs
values={[
	{label: 'macOS', value: 'macos'},
	{label: 'Linux', value: 'linux'}
]}>
<TabItem value="macos">
```md
- others
    ```
    # scale up magic mouse speed
    defaults write -g com.apple.mouse.scaling  6
    ```
- [sudo permission with ID Touch](https://it.digitaino.com/use-touchid-to-authenticate-sudo-on-macos/)

1. Disable spotlight.
2. Delete unused App.
3. Disable Siri.
4. Maximum keyboard and trackpad speed.
5. Adjust control center.
```
</TabItem>

<TabItem value="linux">
```md
@TODO: WIP
```
</TabItem>

<TabItem value="workstation">
```md
@TODO: WIP
```
</TabItem>
</Tabs>

## 2. Setting up a Workstation